A lightweight C application for managing library inventory and book borrowing. It uses a simple menu-driven interface to handle data efficiently.
​Key Features
​Inventory: Add, search, and view book records.
​Issue System: Uses a Queue (FIFO) to process book requests in order.
​Undo Tool: Uses a Stack (LIFO) to quickly delete the most recent entry.
